# Requer TimGcpSmClusterConfig/default (ver timgcpsmclusterconfig-example.yaml).
# Não precisa de projectId no TimGcpSmSecret.
apiVersion: secrets.tim.operator/v1alpha1
kind: TimGcpSmSecret
metadata:
  name: app-secrets
  namespace: default
spec:
  secretId: my-gsm-secret
  secretName: app-secrets
